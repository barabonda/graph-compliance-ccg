.env
.env.*
!.env.example

__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

output/
*.log

.DS_Store
runs/

# evaluate.py 실행 산출물(예측/리포트) — 재실행으로 언제든 재생성되는 결과물이라
# 버전관리 대상이 아니다. eval/ 안의 손으로 쓴 골든 케이스(*_cases.jsonl,
# *_grounding.jsonl, approved_noise_*.json 등)는 이 패턴에 걸리지 않아 계속 추적된다.
eval/*_pred.jsonl
eval/*predictions*.jsonl
eval/*_report*.json
eval/report_*.json
eval/retrieval_sweep_*.json
outputs/ad_images/
_workspace/
_workspace_eval/

# Track C 시드 원본 데이터셋(UnSmile 등). CC-BY-NC-ND 등 라이선스 제약이 있어
# 리포지토리에 재배포하지 않는다. 라이선스·출처는 그래프 노드 속성으로만 각인된다.
# (load_risk_corpus.py 로 언제든 재취득·재적재 가능)
data/risk_corpus/
_workspace_prev_20260704_regression/
_workspace_products/
_workspace_trackc/
~$*.pptx
