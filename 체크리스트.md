# GraphCompliance 정책그래프 강화 — 작업 체크리스트

> 목표: "법 위반은 아니지만 가이드라인 미흡"을 잡는 준법 AI. 핵심은 **정책 그래프에
> 법적 성립요건(legal element)을 제대로 인코딩**해, retrieval + 요건매칭이 판단을
> 결정론적으로 구동하게 하는 것(GraphCompliance 논문 요지). 프롬프트가 아니라 KG.

## 핵심 진단 (2026-07-01 확정)
- retrieval ✅ / feature 감지 ✅ 는 이미 작동.
- **구조적 결함: CULegalElementProfile 374개 중 269개(72%)가 required_features 비어있음.**
- 원인: **Neo4j에 저장된 프로파일이 stale.** 코드 로직(`legal_elements.py`
  `REQUIRED_FEATURES_BY_ACTION_TYPE`)은 이미 올바름(condition_or_scope_missing →
  [unconditional_expression, universal_scope_expression]). 그래프 데이터만 옛날 것.
- 증상: case2("체크카드 있으면 500정도 대출") 오인유발 CU(감독규정 제19조①-1)가
  `action_type=product_fact_mismatch`(오분류) + `required=[]`(빈) → 감지된
  universal_scope_expression이 매칭할 요건이 없어 전부 INSUFFICIENT → needs_review.

## (a) 타깃 증명 — case2 뚫기  ✅ 완료 (2026-07-01)
- [x] policy_compiler 재컴파일 경로: `backfill_legal_element_profiles` (--legal-elements-only-from-existing-profiles)
- [x] 검증: 현재 logic이 오인유발 CU에 올바른 프로파일 생성(condition_or_scope_missing + [unconditional, universal_scope]) — Neo4j 저장값만 stale이었음
- [x] case2 plan의 7개 CU 프로파일 타깃 재컴파일(Cypher 업데이트)
- [x] **case2 라이브 재실행 → reject (needs_review에서 뚫림), NON_COMPLIANT가 감독규정 제19조①-1 정확히 인용**
- [x] 회귀: case1 revise 유지, case3 reject → **3/3 완전 재현**
- 핵심: 프롬프트 무수정. 그래프 요건 인코딩(CU 프로파일)만 고쳐서 판단 구동.

## (b) 확장 — 가드형 재컴파일  ✅ 완료 (2026-07-01)
- [x] naive 전체 재컴파일은 악화 확인(1750 empty, 양질 105개 덮어씀) → 폐기
- [x] **가드형 재컴파일**(scripts_guarded_recompile.py): 양질 보존 + 빈/없는 것만 non-empty 추론 시 기록
- [x] 결과: 요건보유 프로파일 **107→324 (+217)**, 양질 107개 보존, regression 0
- [x] 회귀: regulator_cases 3/3 유지 (v6, workers=1)
- [x] **잔여 1712개 CU = 텍스트로 요건 추론 불가 → 데이터 부실. VLM/LLM 재컴파일 대상**

## (c) 데이터 부실 CU — 진짜 "탄탄하게"  ⬜ 진행중
- [x] JB Drive 마이닝 (2026-07-02): 새 CU 원천 없음. **agent_input_660.jsonl 확보**
      (실제 JB 이벤트페이지 330 + 위험합성 330, gold 비공개) → 우선순위화 substrate
- [x] **retrieval 스윕 120건** (scripts_retrieval_sweep.py, LLM 0회·임베딩만, 194s)
      → eval/retrieval_sweep_120.json. 실전 소환 CU 61종 랭킹 확보
- [x] 스윕이 잡은 그래프 버그 2개 수정:
      ① 가드 재컴파일이 만든 **중복 프로파일 56쌍**(빈것+찬것 혼재 → retrieval이 빈것 집음) 정리
      ② 저장 action_type은 매핑 6종인데 req만 빈 프로파일 **80개 채움**(action_type 기본요건)
      → 매핑형 빈 프로파일 0. 요건보유 프로파일 총 404
- [x] **스윕의 발견**: 실전(JB 이벤트페이지) 최다 소환 = required_disclosure_missing·
      suitability 유형 — positive-feature 요건이 **설계상 없는** 유형(고지체크·게이트 담당).
      즉 남은 빈 프로파일 131개는 대부분 by-design. 진짜 (c) 대상은 ↓
- [x] ~~프로파일 없는 CU 1,501개 LLM 컴파일~~ → **불필요 판명 (2026-07-02)**:
      1,489개는 임베딩도 없는 active_for_gate=False 비활성 레거시(META_CU 1226+ACTOR_CU 414),
      나머지 14개도 비활성 구세대(clause_fcp_*). **활성 retrieval 유니버스(809 CU)는
      전부 legal 프로파일 보유 + 매핑형은 요건 완비.** "1712 데이터부실"은 허수였음.
- [x] regulator 3/3 회귀 확인 (그래프 변경 후, v7: reject/reject/revise ✅)

## 정책그래프 현재 상태 (2026-07-02 결산)
- 활성 CU 809개: 임베딩+legal 프로파일 완비. 매핑 6종 action_type은 요건 100% 채움(404 프로파일)
- 요건 빈 131 프로파일 = disclosure/suitability/절차/제재형 (positive-feature 요건이 설계상 없음)
- 비활성 레거시 1,503 노드: retrieval 불가시. 정리(prune)는 선택사항
- **다음 CU 데이터 프런티어 = 가이드라인 tier**: 은행 광고심의 기준 LegalChunk 적재 +
  제16~18조 grounding(매핑표 완성돼 있음) + 신규 가이드라인 CU 생성 → "법 OK/가이드 미흡" 완성

## (d) 가이드라인 tier 구축  ⬜ 진행중 (2026-07-02 시작)
- [x] **은행 광고심의 기준+세칙 34개조 적재** (load_guideline_corpus.py):
      소스=검증수정본 md(OCR 아님) → ocr_verified=true 정당. authority_tier='guideline'.
      구조 검수: 기준 21/21·세칙 13/13 결번 0, 제16~18조 전수 눈검증, 부칙 중복 제외
- [x] 인용 엣지 21개: 세칙→기준 16(위임), 기준→금소법 제22조·제3조·시행령 제18조
      → breadcrumb 검증: 금소법 제22조 ← 심의기준 제1조(위임근거) 경로 작동
- [x] **대표 근거 선택 로직** (2026-07-02, grounding 선행조건):
      · 발견: rule_judgment.py는 미배선 죽은 모듈, 실제 근거는 router detected_issues
        (하드코딩 "금소법 제22조") + disclosure_catalog.source 단일 문자열 — tier 오귀속 존재
      · disclosure_catalog: 프로파일에 law_basis/guideline_basis 분리(10개 주력 정확 기입,
        loan_rate_basis 법정근거=감독규정 §17①4다1 정정) + resolve_representative_basis():
        부재+법령근거 → 법령 대표 / 가이드-only → 가이드 대표+needs_review cap+"법 위반 아님"
        / 위계미달(prominence) → 심의기준 대표+법령 병기
      · router detected_issues 하드코딩 제거 → 리졸버 배선(authority_tier·co_basis 필드)
      · 단위 3상황 검증 + pytest 실패 목록 baseline과 동일(회귀 0)
- [x] 제16~18조 grounding: 제16조(고지)·제18조4호(prominence)는 코드측 tier 근거로,
      **제17조 각 호(1·2·4·5·7호) → 활성 CU 19개 GROUNDS_CU 엣지** (constraint 키워드 보수 매칭)
      → 데모 breadcrumb 완성: 금소법 §22 → (위임: 심의기준 §1) → 심의기준 §17④ → 적용범위오인 CU
- [x] **tier 배선 E2E 확인** (2026-07-02, eval/tier_e2e_pred.jsonl):
      · PROMINENCE_INSUFFICIENT 이슈가 정확히 목표 형태로 출력 ✅ —
        대표근거=심의기준 제18조제4호, 병기=시행령 제18조④, tier=guideline, sev 3→2,
        사유 선두 "법령 위반이 아닌 심의기준 미흡입니다."
      · 가이드-only 부재 경로(세전/세후)는 fact_match형이라 product_facts 없으면
        NOT_TESTED로 빠져 E2E 미발화(단위검증은 통과). 심의필 MISSING은 벤치마크
        정책(심의필 부재 평가 금지)대로 이슈 미생성 — 정상
- [x] **극성 반전 결함 수정 (2026-07-02)** — 어드바이저 3원칙대로:
      ① 헤지 인식은 그래프 관계(QUALIFIES/MITIGATES) 우선(claim_has_relation_hedge,
        workflow에서 relations 주입) ② 토큰은 '위험 확신 성립'만(ASSERTIVE 명시 단정어
        매칭 시만 feature 발화), 비매칭=판단불충분→judge ③ 헤지 토큰 리스트(최고/최대
        역설 포함) 전체 삭제. canonicalize도 요건당 1 feature(부수어휘 팽창 방지).
      신규 스펙 테스트 2건 green + 회귀 baseline 동일.
      **라이브 검증(4건)**: 클린 needs_review(reject→해소) ✅ / case1 reject ✅ /
      **case2 revise — feature 없이 judge+규제few-shot 단독으로 잡음(아키텍처 실증)** ✅ /
      case3 needs_review ❌(회귀: 보장성→저축성 오인 CU가 은행 도메인 카탈로그에 부재
      추정 — 보험 도메인 CU 필요, follow-up)
- [x] **프론트엔드 tier 반영 (2026-07-02)** — 규범 피라미드 변화의 UI 구현:
      · 백엔드: disclosure_check에 authority_tier/representative_basis/co_basis/tier_note
        방출(product_facts), tier 미기입 프로파일은 tier 미상(오분류 방지)
      · types.ts(DetectedIssue·DisclosureCheck) + selectors(IssueCardModel.authorityTier/coBasis)
      · RiskList 카드: "법령 근거"(적색) vs "심의기준 미흡"(중성) 배지 + 병기 tooltip
      · ExecSummary 머니라인: "법령 근거 N · 심의기준 미흡 M" 분리 카운트
      · tsc clean. 신선한 라이브 예측으로 데이터 관통 확인(심의필 guideline·prominence
        guideline+병기 시행령 §18④)
- [ ] 가이드 고유 조항 신규 CU 생성 (제18조1·2호 등) —
      **⚠ severity/on_missing은 authority_tier에서 자동 유도** (guideline → needs_review,
      절대 법령 CU와 같은 reject 금지 — "법 위반 아님, 심의기준 미흡" 어휘 원칙)
- [x] **규제당국 하위규범 일괄 적재** (2026-07-02, load_regulator_corpus.py — 145개 조항):
      · 공정위 심사지침 4종(63개, guideline·검수됨) + 고시 2종(55개, **law tier** —
        법규보충적 행정규칙이라 심사지침과 권위 구분)
      · 금융광고규제 가이드라인(금융위, VLM OCR 27섹션) — **ocr_verified=false** 표시
      · 인용 엣지 30개(표시광고법 24·금소법 6) — 위임 웹 확장
      · 가이드라인 tier 코퍼스 총 9문서 179개 조항 (은행 심의기준 포함)
- [ ] 신규 적재분의 CU grounding (적재≠도달 교훈 — 심사지침 세부기준 ↔ CU 연결은
      330 파일럿 결과 보고 우선순위 결정)
- [ ] OCR분(금융위 가이드라인) 검수 → ocr_verified=true 전환 + "(원문 확인 필요)" 해제

## JB Drive 인벤토리 (2026-07-02 마이닝)
- 평가벤치마크: agent_input_660.jsonl(로컬 확보) + v0.4 README(gold는 채점자 전용, 미업로드.
  realistic 500/hard_cases 세트 예고). **정책: 심의필 부재를 위반으로 평가 금지** ← disc_review_approval_notice 주의
- 데이터셋: 로컬과 동일 (새 원천 없음)
- 타팀: 우리 팀(JunBub) 리더보드 1위 0.8096. Compliance AI 경쟁 4팀 제안서 보유

## (e) Track B 관계 기반 완화 신호 전환  ✅ 완료 (2026-07-02)
- [x] 발견: Track B가 완화 신호를 정규식 토큰(extract_mitigation_signals)으로 재생성 —
      어드바이저 원칙("그래프 관계 우선")의 미적용 잔재. + Stage1 frame·Stage3 relations가
      Track B에 미주입이었음
- [x] QUALIFIES/MITIGATES 관계 → mitigation_relations로 Track B 프롬프트 주입,
      정규식 함수 삭제(이중 유지보수 제거), workflow에서 relations 전달, types.ts 갱신
- [x] 라이브 검증: 클린(관계 2개 주입: 변동가능성 QUALIFIES·예금자보호 MITIGATES,
      needs_review 유지) ✅ / case2(관계 0개, reject 유지) ✅ — 양극단 모두 정상
- 참고: Track B MEDIUM 점수 0.68 반복은 calibrate_score의 상한 클램프(MEDIUM≤0.68 <
      HIGH문턱 0.75) — MEDIUM은 설계상 revise를 못 밀어올림
- 남은 개선 후보: Stage1 frame(대표 소비자 인상)과 Track B의 중복 판단 통합,
      짧은 광고 적응형 LLM 콜(legacy 단일콜 폴백), context_triples 정리(판정 미사용 파생물)

## (f) 가이드라인 기반 심의 검증 + score 스케일 가드  ✅ (2026-07-02)
- [x] 최종 파이프라인(관계 Track B·frame 참조·적응형 추출·tier)으로 규제당국 판단사례 재검증:
      **case1·2·3 전부 위반 재현 ✅** + 클린 대조군 needs_review ✅ (4/4)
- [x] **신규 버그 발견·수정: LLM judge score 스케일 드리프트** — 한 런에서 0.92/8.6/85.0
      (1점·10점·100점 척도 혼재), router 문턱(0.82→reject)이 그대로 받아 클린 광고 reject.
      수정: JUDGE_SCHEMA에 min/max 경계 + normalize_judgment_score 결정론 가드(judge.py).
      가드 적용 후 클린 재검 → needs_review 복원, NC 0건
- [x] ③ context_triples Neo4j 이중 기록 제거(persistence, 체인 노드가 감사 담당)
- [x] ② 적응형 추출: 문장 추정 <9 legacy 단일콜 / ≥9 staged 자동 승격 —
      양분기 라이브 실증(클린 6문장→legacy, JB 실페이지 131문장→staged)
- [x] ① Stage1 frame → Track B 참조 입력 격상(재검증·정련 지시), 중복 독립판단 해소

## (g) 가이드라인 '원문'의 판정 도달  ✅ 파이프 완결 (2026-07-02)
- [x] **사용자 지적으로 발견**: 검증 4/4에도 불구하고 그래프에 적재한 심의기준 원문이
      판정에 0회 사용(증거 346개 중 0). few-shot·카탈로그 문자열로만 반영되고 있었음
- [x] 원인 2개: ① retriever 증거 쿼리가 GROUNDS_CU 미탐색 ② 캡([0..4]) 경쟁에서 밀림
- [x] 수정: guideline_evidence 전용 수집(캡 독립) → **라이브 실증: judge 증거창에
      "은행 광고심의 기준 제17조: …" 원문 도달, case2 revise 유지**
- [x] judge 프롬프트에 심의기준 병기 지침 추가(법령 대표+심의기준 병기 / 심의기준-only면
      "법령 위반이 아닌 심의기준 미흡" 명시) — 병기 인용은 다음 라이브에서 확인
- 교훈: "grounding 엣지 존재"≠"판정 도달". 소비 경로(retriever 쿼리)까지 검증해야 함

## 운영 노트
- Neo4j Aura: 라이브 평가 시 workers 높으면 "Unable to retrieve routing information" 간헐 발생 → **workers=1 권장** 또는 재시도 로직 필요. auto-pause도 주의.

## 데이터 파이프라인 (CU 품질 = 데이터 품질)
- 원천: JB Google Drive (https://drive.google.com/drive/folders/1vy2oTE4V_s9A3fZa8cwqwdEgNuBOqSfI) — Drive MCP로 접근 가능
- 법령/가이드/심의기준: JB 해커톤 데이터셋(로컬) — 은행 광고심의 기준, 금융광고규제 가이드라인 OCR 완료
- **파싱 부실분 → VLM(unlimited_glue, gundam+grounding) 재파싱** (표/이미지 광고)
- 심사지침 위반 예시(126건, 대부분 `<img>`) → 방안③ 멀티모달 후보

## 완료됨 (이번 세션)
- [x] 채점기 정규화 버그 수정 (금소법↔정식명, 조 단위) → article F1 0→0.33
- [x] 법령 간 위임 엣지 225개 구축 (build_cross_law_citations.py)
- [x] legal_hierarchy.py — 하위규정 인용 시 모법 병기 (router 연결)
- [x] disc_loan_rate_basis 룰 — teaser 금리 갭 (규제당국 p17 Q1로 검증)
- [x] regulator_cases.jsonl (판단사례 3건) + regulator_grounding.jsonl (근거 5개)
- [x] 가이드라인 28p VLM OCR (표 colspan/rowspan 복원)
- [x] 방안③ prominence-on-image PoC (scripts/prominence_on_crop.py)
- [x] case1 라이브 재현 ✅ (teaser 금리 → reject, 금소법 제22조)

## 재현 현황 (post-(a)+(b), v6)
| 사례 | gold | 결과 |
|---|---|---|
| case1 teaser 금리 | 위반 | ✅ reject |
| case2 적용범위 오인 | 위반 | ✅ reject (needs_review에서 뚫림) |
| case3 저축성 오인 | 위반 | ✅ revise |
→ **3/3 완전 재현.** 프롬프트 무수정, 그래프 요건 인코딩만으로 달성.

## (h) 심사 코파일럿 — CopilotKit×LangGraph (2026-07-03)
- [x] LangGraph 에이전트(copilot_agent.py) + AG-UI FastAPI 마운트(/copilot-agent)
- [x] Next 런타임(app/api/copilotkit) — rewrites가 동적 라우트보다 먼저라 copilotkit 제외 처리
- [x] CopilotSidebar(420px 도킹) + useAgentContext(원문·요건별판단·고지현황 포함)
- [x] 데이터 거버넌스 레이어(copilot_graph_tools.py): 읽기전용·ws격리·행수상한·감사추적
  - 템플릿 5종(규정검색/조문원문/심의기준/상품사실/문서목록) + T2C(graph_query, 검증+1회재생성)
- [x] 프롬프트: 조문 인용 의무, 법/심의기준 구분, OCR 미검수 "(원문 확인 필요)", JSON 키 자연어화
- [ ] terminated 콘솔 노이즈: 콘텐츠 전달은 정상, RUN_FINISHED 후 발생. Connection:close 무효
      → 유휴 재연결 루프 여부 진단 중 (CopilotKit#2402 계열, dev 전용 가능성)
- [ ] Phase 2: 프론트 도구(카드 선택·원칙 필터) — 바인딩 경로는 이미 구현됨

## (i) kunwoo 캄보디아(PPCBank) 다국어 확장 통합 (2026-07-04)
- [x] origin/kunwoo 머지 — INTEGRATION_KUNWOO.md 지침대로 충돌 6건 의미 단위 해소
  · judge/router: uses_korean_law_context 게이트(비-KR 한국법 미주입, KR few-shot KR 전용)
  · product_facts: 선적재 로더 이원화(KH=env게이트 CONTAINS_FACT / KR=기존 HAS_PRODUCT_FACT)
  · 프론트: 새 콘솔에 번역 재이식(원문 줄별 EN/KO·카드 인용 번역·교차언어 패널)
- [x] 새 심사 은행 선택: 전북은행/광주은행(KR ws) · 프놈펜상업은행(KH ws, en)
- [x] 2-DB 배선: NEO4J_*→kunwoo 샌드박스(7349cbb2, KH 그래프+심사 산출물),
  TEAM_NEO4J_*→팀 Aura(KR 코퍼스 읽기). copilot_graph_tools도 팀 라우팅 패치
- [x] KH 정렬 게이트 임계값: CCG_MIN_{HYPERNYM,PREMISE}_<kh_ws>=10/15 (KH 13/19 실측)
- [x] 검증: 샌드박스 KH 그래프 실측 일치(CU16·PF255·Hyp13), KR copilot 팀 라우팅 정상,
  pytest 신규 실패 0. 라우팅 미배선 시 KH 심사가 policy_alignment_missing으로 정확히
  차단되는 것도 확인(게이트 동작 증명)
- 주의: 운영 대시보드 run 목록이 이제 샌드박스 기준(과거 KR run은 팀 Aura에 남음)
- [ ] KH 라이브 E2E 다국어 화면 확인 (진행 중)
